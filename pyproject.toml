[project]
name = "alumnium_tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alumnium>=0.16.0",
    "pytest>=9.0.1",
    "pytest-order>=1.3.0",
    "pytest-xdist>=3.8.0",
    "selenium>=4.38.0",
    "webdriver-manager>=4.0.2",
]
